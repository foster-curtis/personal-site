@AGENTS.md

## Claude Code

- `.claude/rules/*.md` are path-scoped and load automatically when you touch matching
  files — don't ask for or re-paste their content here.
- `.claude/skills/check` and `.claude/skills/new-api-route` are available and can be
  invoked directly with `/check` and `/new-api-route`.
